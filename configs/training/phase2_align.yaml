training:
  phase: phase2
  learning_rate: 1.0e-4
  weight_decay: 0.01
  max_grad_norm: 1.0
  batch_size: 4
  max_steps: 2000
  log_every: 10
  save_every: 100
  output_dir: outputs/phase2
  vocab_size: 128000
data:
  source: toy
  n_samples: 100
  seed: 42
  max_seq_len: 512
model_config: configs/model/sky_v1_1B.yaml
